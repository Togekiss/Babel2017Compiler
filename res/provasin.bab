const CONSTANT = 5;
const hola = "Benvingut!";

var id: sencer;
var boolea: logic;

funcio lamevafuncio(perref id:sencer): logic;
func
	si (id > 1) llavors
		retornar cert;
	sino
		retornar fals;
	fisi;
fifunc;


prog

escriure(hola);
llegir(boolea);
id =  si (cert)?22:255*0;
boolea = lamevafuncio(id);

id = 0;
mentre id < 10 fer
	escriure(id);
	id = (id + 1);
fimentre;

cicle
	id = id - 1;
fins (i == 0);


fiprog