const CONSTANT = 5;

var id: vector [0 .. 24] de sencer;

funcio lamevafuncio (perref id:sencer): logic;
func
	si (id > 1) llavors
		retornar cert;
	sino
		retornar fals;
	fisi;
fifunc;


prog 

id = 0;
mentre id < 10 fer
	escriure(id);
	id = (id + 1);
fimentre;


id[sempre] = ds;

fiprog